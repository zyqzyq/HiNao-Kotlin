<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <!--状态栏 color 测试机子未看到效果-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--actionbar color 暂未用到-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--windowsBackground color -->
        <item name="android:windowBackground">@color/colorWindowBg</item>
        <!--底部导航栏颜色 暂时无效-->
        <item name="android:navigationBarColor">@color/colorNavigation</item>
        <!--Text color size -->
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:textSize">@dimen/sayTextSize</item>
        <!--各个控制控件的默认颜色-->
        <item name="colorControlNormal">@color/colorControlNormal</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 设置该属性解决空白部分-->
        <item name="contentInsetStart">0dp</item>
    </style>
    <!--preferenceScreen theme-->
    <style name="preferenceScreenTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/colorPreferenceBg</item>  <!-- 窗口背景 -->
        <item name="android:focusable">false</item>
        <item name="android:textColor">@color/textColorPrimary</item> <!-- 字体颜色 -->
        <item name="android:textSize">@dimen/sayTextSize</item>
        <item name="android:colorBackgroundCacheHint">#00000000</item> <!-- 背景缓存色透明，避免拖动全黑 -->
        <item name="android:windowNoTitle">true</item> <!-- 是否有标题 -->
    </style>
    <style name="button_style">
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">@dimen/BtnTextSize</item>
        <item name="android:backgroundTint">@color/colorBtnBg</item>
    </style>
    <style name="imageBtn_style">
        <item name="android:layout_gravity">center</item>
        <item name="android:background">@color/colorWindowBg</item>
    </style>
    <style name="button2_style">
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">@dimen/BtnTextSize</item>
        <item name="android:background">@color/colorBtnBg2</item>
    </style>
</resources>
